-- Verificar el nombre del jugador que ejecutó el código
local jugador = game.Players.LocalPlayer

-- Nombre del jugador que deseas verificar
local nombreJugador = "Plaga_225"

if jugador.Name == nombreJugador then
    print("FORCEHUB EL PITBULL DE LOS HACKS.") 
print("ForceHub Iniciando.")
print("ForceHub Gaming.")

game:GetService("StarterGui"):SetCore("SendNotification", {
    Title = "🐕‍🦺ForceHub🐕‍🦺",
    Text = "Cargando script.",
    Icon = "", -- Puedes colocar aquí el asset ID de un ícono opcional
    Duration = 5 -- Duración en segundos (opcional)
})
wait(2)
game:GetService("StarterGui"):SetCore("SendNotification", {
    Title = "🐕‍🦺ForceHub🐕‍🦺",
    Text = "ForceHub Indetectable.",
    Icon = "", -- Puedes colocar aquí el asset ID de un ícono opcional
    Duration = 5 -- Duración en segundos (opcional)
})
wait(2)

local Library = loadstring(game:HttpGet("https://pastebin.com/raw/vff1bQ9F"))()
local Window = Library.CreateLib("🐕‍🦺ForceHub Tower Of Hell Premium🐕‍🦺 ", "BloodTheme")

local JUGADOR = Window:NewTab("Inicio")
local JUGADORSection0 = JUGADOR:NewSection("Bienvenido.")
local JUGADORSection0 = JUGADOR:NewSection("Versión Premium.")
local JUGADORSection0 = JUGADOR:NewSection("YouTube:Hackbull.")
local JUGADORSection0 = JUGADOR:NewSection("TikTok:Hackbull.")

local JUGADOR = Window:NewTab("Jugador")
local JUGADORSection1 = JUGADOR:NewSection("Herramientas para el jugador.")

local JUGADOR = Window:NewTab("Herramientas")
local JUGADORSection2 = JUGADOR:NewSection("Herramienta para ganar.")

local JUGADOR = Window:NewTab("Extras")
local JUGADORSection3 = JUGADOR:NewSection("Herramienta extras.")

JUGADORSection1:NewSlider("Walkspeed.", "Speed Change", 250, 16, function(s) -- 250 (MaxValue) | 0 (MinValue)
    game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = s
end)

JUGADORSection1:NewSlider("Jump Power.", "Jump Power Change", 500, 50, function(s) -- 250 (MaxValue) | 0 (MinValue)
    game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = s
end)

JUGADORSection1:NewButton("Infinite Jump.", "Esta opción te da salto infinito.", function()
        local thing = Instance.new("Part")
        thing.Name = "This is a part of the game"
        thing.Anchored = true
        thing.Parent = game.Workspace
        thing.Size = Vector3.new(5, 0.1, 5)
        thing.Transparency = 1
    game:GetService("RunService").Stepped:connect(
            function()
            local pos = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame
        thing.CFrame = CFrame.new(pos.X, pos.Y-3.3,pos.Z)--beetween -3.1 and idk but prob -3.6
        end)
end)

JUGADORSection1:NewButton("GoodMode.", "Esta opción de un un Good Mode para evitar tu muerte.", function(state) 
local LocalPlayer = game:GetService("Players").LocalPlayer
 
local function Invincibility()
    if LocalPlayer.Character then
        for i, v in pairs(LocalPlayer.Character:GetChildren()) do
            if v.Name == "hitbox" then
                v:Destroy()
            end
        end
    end
end
 
while wait(0.5) do
    Invincibility(LocalPlayer)
end

end)

JUGADORSection2:NewButton("AutoGanar.", "Esta opción hace que ganes al momento.", function(state)

local P = game:GetService'Players'
local HRP = P.LocalPlayer.Character.HumanoidRootPart
local Sections = game:GetService("Workspace").tower.sections
 
for _, Child in next, Sections:GetChildren() do
    if Child.Name ~= "start" then
        HRP.CFrame = Child.start.CFrame + Vector3.new(1,2,1)
        task.wait()
    end
end
HRP.CFrame = Sections.finish.FinishGlow.CFrame

end)

JUGADORSection2:NewButton("Obtener todas las herramientas.", "Esta opción te las herramientas de tower of hell.", function(state)

for _,v in pairs(game.Players.LocalPlayer.Backpack:GetDescendants()) do
        if v:IsA("Tool") then
            v:Destroy()
        end
    end
    wait() 
    for _,v in pairs(game.ReplicatedStorage.Gear:GetDescendants()) do
        if v:IsA("Tool") then
            local CloneThings = v:Clone()
            wait()
            CloneThings.Parent = game.Players.LocalPlayer.Backpack
        end
    end

end)

JUGADORSection3:NewButton("Infinite Yield.", "Esta opción es un script admin FE.", function(state)
loadstring(game:HttpGet(('https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source'),true))()
end)

JUGADORSection3:NewButton("Psy.", "Esta opción es un script ejecutor.", function(state)
loadstring(game:GetObjects("rbxassetid://3014051754")[1].Source)()
end)


JUGADORSection3:NewButton("ESP.", "Esta opción es un script para visualizar a los jugadores a través de las paredes.", function(state)
loadstring(game:HttpGet('https://raw.githubusercontent.com/Lucasfin000/SpaceHub/main/UESP'))()
end)


JUGADORSection3:NewButton("Abrir Consola.", "Esta opción hace que se abra la consola.", function(state)
game.StarterGui:SetCore("DevConsoleVisible", true)
    wait()
end)

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "ScreenGui"
ScreenGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")
ScreenGui.ResetOnSpawn = false
 
local Toggle = Instance.new("TextButton")
Toggle.Name = "🐾"
Toggle.Parent = ScreenGui
Toggle.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
Toggle.Position = UDim2.new(0, 0, 0.454706937, 0)
Toggle.Size = UDim2.new(0, 90, 0, 38)
Toggle.Font = Enum.Font.SourceSans
Toggle.Text = "🐾"
Toggle.TextColor3 = Color3.fromRGB(248, 248, 248)
Toggle.TextSize = 28.000
Toggle.Draggable = true
Toggle.MouseButton1Click:connect(function()
    Library:ToggleUI()
end)
else
    print("FORCEHUB EL PITBULL DE LOS HACKS.") 
print("ForceHub Iniciando.")
print("ForceHub Gaming.")

game:GetService("StarterGui"):SetCore("SendNotification", {
    Title = "🐕‍🦺ForceHub🐕‍🦺",
    Text = "Cargando script.",
    Icon = "", -- Puedes colocar aquí el asset ID de un ícono opcional
    Duration = 5 -- Duración en segundos (opcional)
})
wait(2)
game:GetService("StarterGui"):SetCore("SendNotification", {
    Title = "🐕‍🦺ForceHub🐕‍🦺",
    Text = "ForceHub Indetectable.",
    Icon = "", -- Puedes colocar aquí el asset ID de un ícono opcional
    Duration = 5 -- Duración en segundos (opcional)
})
wait(2)

local Library = loadstring(game:HttpGet("https://pastebin.com/raw/vff1bQ9F"))()
local Window = Library.CreateLib("🐕‍🦺ForceHub Tower Of Hell Gratis🐕‍🦺 ", "BloodTheme")

--------------------------------------------------------------------------------------BOTONES----------------------------------------------------------------------------------------
local JUGADOR = Window:NewTab("Inicio")
local JUGADORSection0 = JUGADOR:NewSection("Bienvenido.")
local JUGADORSection0 = JUGADOR:NewSection("Versión Gratis.")
local JUGADORSection0 = JUGADOR:NewSection("YouTube:Hackbull.")
local JUGADORSection0 = JUGADOR:NewSection("TikTok:Hackbull.")

local JUGADOR = Window:NewTab("Jugador")
local JUGADORSection1 = JUGADOR:NewSection("Herramientas para el jugador.")

local JUGADOR = Window:NewTab("Herramientas")
local JUGADORSection2 = JUGADOR:NewSection("Herramienta para ganar.")

local JUGADOR = Window:NewTab("Extras")
local JUGADORSection3 = JUGADOR:NewSection("Herramienta extras.")

JUGADORSection1:NewSlider("Walkspeed.", "Speed Change", 250, 16, function(s) -- 250 (MaxValue) | 0 (MinValue)
    game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = s
end)

JUGADORSection1:NewSlider("Jump Power.", "Jump Power Change", 500, 50, function(s) -- 250 (MaxValue) | 0 (MinValue)
    game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = s
end)

JUGADORSection1:NewButton("GoodMode.", "Esta opción de un un Good Mode para evitar tu muerte.", function(state) 
local LocalPlayer = game:GetService("Players").LocalPlayer
 
local function Invincibility()
    if LocalPlayer.Character then
        for i, v in pairs(LocalPlayer.Character:GetChildren()) do
            if v.Name == "hitbox" then
                v:Destroy()
            end
        end
    end
end
 
while wait(0.5) do
    Invincibility(LocalPlayer)
end

end)

JUGADORSection2:NewButton("AutoGanar.", "Esta opción hace que ganes al momento.", function(state)

local P = game:GetService'Players'
local HRP = P.LocalPlayer.Character.HumanoidRootPart
local Sections = game:GetService("Workspace").tower.sections
 
for _, Child in next, Sections:GetChildren() do
    if Child.Name ~= "start" then
        HRP.CFrame = Child.start.CFrame + Vector3.new(1,2,1)
        task.wait()
    end
end
HRP.CFrame = Sections.finish.FinishGlow.CFrame

end)

JUGADORSection3:NewButton("Infinite Yield.", "Esta opción es un script admin FE.", function(state)
loadstring(game:HttpGet(('https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source'),true))()
end)

JUGADORSection3:NewButton("Abrir Consola.", "Esta opción hace que se abra la consola.", function(state)
game.StarterGui:SetCore("DevConsoleVisible", true)
    wait()
end)

--------------------------------------------------------------------------------------BOTÓN DE UI----------------------------------------------------------------------------------------
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "ScreenGui"
ScreenGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")
ScreenGui.ResetOnSpawn = false
 
local Toggle = Instance.new("TextButton")
Toggle.Name = "🐾"
Toggle.Parent = ScreenGui
Toggle.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
Toggle.Position = UDim2.new(0, 0, 0.454706937, 0)
Toggle.Size = UDim2.new(0, 90, 0, 38)
Toggle.Font = Enum.Font.SourceSans
Toggle.Text = "🐾"
Toggle.TextColor3 = Color3.fromRGB(248, 248, 248)
Toggle.TextSize = 28.000
Toggle.Draggable = true
Toggle.MouseButton1Click:connect(function()
    Library:ToggleUI()
end)
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
end
